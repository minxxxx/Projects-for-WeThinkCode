<p id="notice"><%= notice %></p>

<div id="nb_books">
  <h1 float: left;>Number of Books</h1> <h1 id="nb"> <%= @books.count %> </h1>
</div>

<table>
  <thead>
    <tr>
      <th>Name</th>
      <th colspan="3"></th>
    </tr>
  </br></br>
  </thead>

  <tbody id="books_update">
    <% @books.each do |book| %>
      <tr id= <%= book.id %>>
        <td><%= book.name %></td>
        <td><%= link_to 'Show', book %></td>
        <td><%= link_to 'Edit', edit_book_path(book),
                :remote => true, :class => 'edit_book'  %></td>
        <td><%= link_to 'Destroy', book, method: :delete, data: { confirm: 'Are you sure?' },
                :remote => true, :class => 'delete_book' %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>
<div id="error_explanation" style="display: none">
  <h2 id ="nberrors"> </h2>
</div>

<div id="form_new">
</div>
<div id="form_edit">
</div>
<%= link_to 'New Book', new_book_path, remote: true, class: "btn" %>
