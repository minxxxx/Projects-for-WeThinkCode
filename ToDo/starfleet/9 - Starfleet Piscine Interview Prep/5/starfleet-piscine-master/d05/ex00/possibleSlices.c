
#include "header.h"

void	findPossibleSlices(t_arr *list, int index, int pizzaSize, int reduced)
{
	if (reduced < 0)
		return ;
	// if (reduced == 0)
	// {

	// }
	int	prev = (index == 0) ? 1: list[]
}

void	printPossibleSlices(int pizzaSize)
{
	t_arr	*list = arrayInit();
}