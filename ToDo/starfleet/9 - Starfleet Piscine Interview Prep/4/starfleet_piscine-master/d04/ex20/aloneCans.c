#include "header.h"

void aloneCans(int *arr, int n)
{
	int can1 = 0;
	int can2 = 0;
	
	for(int i = 0; i < n; i++)
	{
		if (can > can ^ arr[i])
			can ^= arr[i];
		else


	}
	printf("%d\n", can1);
	printf("%d\n", can2);
}